"""Shared decision policies embedded in short-horizon agent prompts."""

SHORT_HORIZON_DECISION_POLICY = """
# 头部科技股短周期决策规则（强制）

- 本规则专门面向超大市值/头部科技股，最终方向只预测未来 **1-5 个交易日**。必须区分长期基本面与短期催化；长期估值只能影响情景、仓位和失效条件，不能单独推出短期交易方向。
- 对关键证据执行来源去重和事件去重：记录来源、发布时间/数据时点及原始事实；同一新闻转载、同一机构报告的二次转述、同一事件的多篇摘要只算一个证据，不得用重复叙事虚增支持度。
- 采用结论前必须主动列出最强反证、数据缺口和另一条可行路径，说明为何当前仍采纳主情景。辩论修辞强弱不等于证据强弱。
- 改变上游方向时，必须给出新增或被遗漏证据的可核验来源、反证处理和明确失效条件。置信度应由证据质量、独立性、时效性和冲突程度决定，不使用固定数值上限。
- 可核验的事件驱动信息若改变盈利预期、流动性、监管约束或供需结构，可以在尚无右侧价格确认时触发市场状态切换；此时必须说明传导机制、反向情景和可执行的失效条件。
- 财报等二元事件前必须区分已定价预期与新增信息，并同时分析正反两条事件路径。禁止把“保护资本”“避免踏空”等口号当作方向证据。
"""

MARKET_REGIME_DECISION_POLICY = """
# 头部科技股市场状态双向解释规则（强制）

- VIX 急涨既可能代表恐慌抛售，也可能触发做市商对冲回补、空头回补和随后均值回归。必须结合个股相对强弱、关键价位和期权持仓结构分析，禁止将 VIX 单向映射为下跌。
- 若期权数据缺少可比较的隐含波动率或期限结构，必须明确“波动率风险溢价未验证”，不得声称期权市场已经确认方向。
- 缩量下跌必须同时评估弱势延续与抛压枯竭两条路径。放量破位、OBV 恶化或支撑失守是可用证据，但都要检查数据来源、重复事件和相反证据，不能把单一指标设为唯一确认条件。
- 财报、监管、产品发布、供应链变化等事件驱动信息若实质改变状态，可以在价格确认前调整方向；必须给出事件来源、因果链、反向情景与失效条件。
- 极端超买或超卖只能提示均值回归可能性，不能替代成交量、支撑阻力、事件催化及反证审查。
"""

EXECUTION_PLAN_DECISION_POLICY = """
# 六类交易意图与可执行价格规则（强制）

- Trader 和 Portfolio Manager 必须明确输出 `trade_intent`，且只能是 `open_long`、`reduce_long`、`close_long`、`open_short`、`cover_short`、`hold` 之一。动作映射为：buy=`open_long|cover_short`，sell=`reduce_long|close_long|open_short`，hold=`hold`。
- `open_long`：给出代表入场价和完整入场区间、目标、止损、`time_stop_days`；止损低于区间下沿，目标高于区间上沿，`position_advice` 必须是 `light/medium/heavy`。
- `open_short`：给出代表入场价和完整入场区间、目标、止损、`time_stop_days`；目标低于区间下沿，止损高于区间上沿，`position_advice` 必须是 `light/medium/heavy`。
- `reduce_long`：给出减仓代表价和完整执行区间；若减仓后仍有仓位（`position_advice != none`），还要给出高于区间的剩余仓位目标、低于区间的防守止损和 `time_stop_days`；无剩余仓位时 `time_stop_days` 留空。
- `close_long` 与 `cover_short`：给出代表成交价和完整执行区间，`position_advice` 必须为 `none`；退出后没有剩余仓位，不得虚构目标价或止损价，无剩余仓位时 `time_stop_days` 留空。
- `hold`：无仓位观望时无需填写执行价格或 `time_stop_days`；持有现有仓位时必须给出目标、止损和 `time_stop_days`，但不得把条件单伪装成当前开仓意图。
- 入场/执行区间必须覆盖现实可成交位置并支持分批执行。所有调整都要说明证据来源、反证和失效条件；不得通过不现实价格凑盈亏比。
"""
